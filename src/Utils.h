#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <algorithm>

namespace Utils {
    std::string toLower(const std::string& str);
    std::string cleanWord(const std::string& word);
};


#endif